# Copy to `.env.local` (gitignored). Server-side only -- never exposed to the browser
# (no NEXT_PUBLIC_ prefix), so the dashboard reads Supabase Postgres directly from
# server components. Same connection string the analytics layer uses.
SUPABASE_DB_URL=postgresql://postgres:YOUR_PASSWORD@aws-1-REGION.pooler.supabase.com:5432/postgres
